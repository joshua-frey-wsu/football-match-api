# football-match-api
This Project uses the AI from defang generate to build a node.js project that displays a table of the champions league matches from [football-data.org's](https://www.football-data.org/) API.
